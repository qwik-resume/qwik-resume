import { describe, test } from 'vitest';
import { TagList } from './tag-list';

describe('TagList', () => {
  test.skip('creates a basic control for entering a list of tags', async () => {
    // Test code goes here
  });

  test.skip('displays an array of strings as a comma-separated list in the control', async () => {
    // Test code goes here
  });

  test.skip('converts user input of comma-separated items back into an array', async () => {
    // Test code goes here
  });

  test.skip(
    'triggers event when user updates list that returns the new array of tags',
    async () => {
      // Test code goes here
    },
  );

  test.skip('user changes update original array of strings that was provided', async () => {

  });
});
